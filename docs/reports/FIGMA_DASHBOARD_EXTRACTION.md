# Figma 대시보드 디자인 추출 문서

## 📋 개요

이 문서는 Figma의 삼성-연돌시스템-디자인 파일에서 **대시보드(Node ID: 19:4148)** 프레임을 분석하여 추출한 Design Tokens, 컴포넌트, 그리고 레이아웃 정보를 정리한 것입니다.

---

## 🎨 Design Tokens

### 색상 (Colors)

#### Primary Colors
- **Primary 90**: `#004594` - 주요 텍스트, 로고 색상
- **Primary Default**: `#007bee` - 링크, 강조 색상

#### Secondary Colors
- **Secondary 30**: `#aae8f1` - 활성 태그 배경
- **Secondary Pressed**: `#84deea` - 인터랙티브 요소 호버 상태

#### Text Colors
| 용도 | 색상 | HEX |
|------|------|-----|
| Primary (기본) | 진한 회색 | `#131416` |
| Secondary (부제목) | 중간 회색 | `#464c53` |
| Disabled | 연한 회색 | `#aeb9c2` |
| Action | 파란색 | `#007bee` |
| White | 흰색 | `#ffffff` |
| Unselected | 보조 회색 | `#6d7882` |

#### Background Colors
- **Background White**: `#ffffff` - 카드 배경
- **Background Gray Subtle**: `#e0e5ea` - 섹션 배경
- **Background Gray Subtlest**: `#f4f5f6` - 강조 배경

#### Status Colors
| 상태 | 배경 | 텍스트 | 용도 |
|------|------|--------|------|
| Success | `#edfff0` | `#28cf47` | 정상/활성화 |
| Warning | `#fff3cd` | `#856404` | 경고 상태 |
| Error | `#f8d7da` | `#721c24` | 오류 상태 |

#### Chart Colors
- **Safe (Blue)**: `#223e92` - 안전 상태 (차압 낮음)
- **Safe Light**: `rgba(34,62,146,0.45)` - 안전 상태 (투명)
- **Warning (Red)**: `#b11e24` - 경고 상태 (차압 높음)
- **Warning Light**: `rgba(177,30,36,0.45)` - 경고 상태 (투명)
- **Neutral Green**: `#2bb469` - 중성대 표시

### 타이포그래피 (Typography)

#### Title
```
- xlarge: 56px, 500, 67.2px line height
- large: 48px, 500, 57.6px line height  
- regular: 40px, 500, 48px line height
```

#### Subtitle
```
- large: 32px, 500, 44px line height
- regular: 24px, 500, 1.2 line height
```

#### Body
```
- large: 20px, 500, 1.2 line height
- regular: 16px, 400, 24px line height
- regular_m: 16px, 500, 1.2 line height
```

#### Element
```
- large: 14px, 400, 1.2 line height
- medium: 12px, 400, 16px line height
- medium_m: 12px, 500, 16px line height
```

**기본 폰트**: `Pretendard`, `Spoqa Han Sans Neo`

### 간격 (Spacing)

| 이름 | 값 |
|------|-----|
| gap-1 | 2px |
| gap-2 | 4px |
| gap-section | 24px |
| padding-4 | 8px |
| padding-6 | 12px |
| padding-card | 24px |
| padding-section | 32px |

### 경계 반경 (Border Radius)

| 이름 | 값 |
|------|-----|
| xxlarge | 12px |
| large | 8px |
| max (rounded) | 1000px |
| small | 6px |

---

## 🧩 재사용 가능한 컴포넌트

### 1. Tag 컴포넌트

**설명**: 상태를 시각적으로 표시하는 태그 컴포넌트

**Props**:
```typescript
interface TagProps {
  state?: 'default' | 'active' | 'success' | 'warning';  // 기본값: 'default'
  label: string;                                           // 표시할 텍스트
  className?: string;                                      // 추가 CSS 클래스
  onClick?: () => void;                                    // 클릭 이벤트
}
```

**변종 (Variants)**:
- **default**: 투명 배경, 검정 텍스트
- **active**: `#aae8f1` 배경, 검정 텍스트
- **success**: `#edfff0` 배경, `#28cf47` 텍스트
- **warning**: `#fff3cd` 배경, `#856404` 텍스트

**스타일**:
```css
height: 24px;
padding: 0 12px;
border-radius: 1000px;
font-weight: 500;
font-size: 12px;
line-height: 16px;
```

**사용 예시**:
```typescript
import { Tag } from '@/components';

// 기본
<Tag label="온도" />

// 활성
<Tag label="습도" state="active" />

// 성공 상태
<Tag label="정상" state="success" />
```

---

### 2. DataCard 컴포넌트

**설명**: 데이터를 표시하는 일반 카드 컴포넌트

**Props**:
```typescript
interface DataCardProps {
  title: string;              // 카드 제목
  subtitle?: string;          // 선택적 부제목
  children: React.ReactNode;  // 카드 내용
  className?: string;         // 추가 CSS 클래스
  headerAction?: React.ReactNode;  // 헤더 우측 액션
}
```

**스타일**:
```css
background: #ffffff;
border: 1px solid #aeb9c2;
border-radius: 12px;
padding: 24px;
gap: 24px;
```

**사용 예시**:
```typescript
import { DataCard } from '@/components';

<DataCard
  title="날씨"
  subtitle="현재"
  headerAction={<button>새로고침</button>}
>
  <div>온도: 25°C</div>
</DataCard>
```

---

### 3. StatusBadge 컴포넌트

**설명**: 상태 배지를 표시하는 컴포넌트

**Props**:
```typescript
interface StatusBadgeProps {
  status: 'success' | 'warning' | 'active' | 'error';
  label: string;
  className?: string;
}
```

**상태별 색상**:
| 상태 | 배경 | 텍스트 |
|------|------|--------|
| success | `#edfff0` | `#28cf47` |
| active | `#edfff0` | `#28cf47` |
| warning | `#fff3cd` | `#856404` |
| error | `#f8d7da` | `#721c24` |

**사용 예시**:
```typescript
import { StatusBadge } from '@/components';

<StatusBadge status="success" label="정상" />
<StatusBadge status="active" label="활성화" />
```

---

### 4. WeatherCard 컴포넌트

**설명**: 날씨 정보를 표시하는 카드 컴포넌트

**Props**:
```typescript
interface WeatherCardProps {
  title: string;           // 섹션 제목 (예: "현재 날씨")
  temperature: number;     // 온도 (°C)
  humidity: number;        // 습도 (%)
  windSpeed: number;       // 풍속 (m/s)
  weatherIcon?: React.ReactNode;  // 날씨 아이콘
  className?: string;      // 추가 CSS 클래스
}
```

**데이터 포인트**:
- 온도 (°C)
- 습도 (%)
- 풍속 (m/s)

**스타일**:
```css
background: #f4f5f6;
border: 1px solid #ced5db;
border-radius: 6px;
padding: 24px;
```

**사용 예시**:
```typescript
import { WeatherCard } from '@/components';

<WeatherCard
  title="현재 날씨"
  temperature={25}
  humidity={45}
  windSpeed={3.2}
/>
```

---

### 5. Heatmap 컴포넌트

**설명**: 층별 차압을 시각화하는 히트맵 컴포넌트

**Props**:
```typescript
interface HeatmapProps {
  title: string;
  levels: Array<{
    floor: string;    // 층 (8F, 7F, ...)
    value: number;    // 차압 값
    color: string;    // 색상 (HEX)
  }>;
  unit?: string;      // 단위 (기본값: "Pa")
  legendItems?: Array<{ color: string; label: string }>;
  className?: string;
}
```

**색상 스케일**:
```
Safe (낮음): #223e92 → rgba(34,62,146,0.45)
Warning (높음): #b11e24 → rgba(177,30,36,0.45)
Neutral: #ffffff with #2bb469 border
```

**사용 예시**:
```typescript
import { Heatmap } from '@/components';

<Heatmap
  title="히트맵"
  levels={[
    { floor: '8F', value: -26.2, color: '#223e92' },
    { floor: '7F', value: -23.2, color: '#223e92' },
    { floor: '6F', value: -16.2, color: 'rgba(34,62,146,0.7)' },
    { floor: '4F', value: -2.1, color: '#ffffff', border: '#2bb469' },
  ]}
  unit="Pa"
/>
```

---

## 📐 페이지 레이아웃 구조

### 대시보드 페이지 (1920px width)

```
┌─────────────────────────────────────────┐
│         GNB (65px, fixed)               │
├─────────────────────────────────────────┤
│                                         │
│  Header (29px)                          │
│  - 페이지 제목: "대시보드"               │
│  - 마지막 업데이트: "2026-01-07(수) 오후 10:34:51" │
│                                         │
├─────────────────────────────────────────┤
│  Weather & Sensor (249px)               │
│  ┌─────────────────┐  ┌─────────────────┐
│  │  날씨 카드      │  │  차압/센서      │
│  │  (현재/내일)    │  │  정보           │
│  └─────────────────┘  └─────────────────┘
│                                         │
├─────────────────────────────────────────┤
│  Pressure Data (680px)                  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────┐
│  │  히트맵     │ │  압력 프로   │ │상세 │
│  │  (8F-1F)    │ │  파일 차트   │ │데이 │
│  │             │ │             │ │터   │
│  └─────────────┘ └─────────────┘ │표   │
│                                   │   │
│                                   └─────┘
│                                         │
├─────────────────────────────────────────┤
│  Charts (426px) - 3 Column Layout       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐
│  │  기압    │ │  기온    │ │  습도    │
│  │  그래프  │ │  그래프  │ │  그래프  │
│  └──────────┘ └──────────┘ └──────────┘
│                                         │
└─────────────────────────────────────────┘
```

### 섹션별 정보

| 섹션 | 높이 | 컬럼 수 | 내용 |
|------|------|--------|------|
| GNB | 65px | 1 | 로고, 네비게이션, 사용자 정보 |
| Header | 29px | 1 | 페이지 제목, 업데이트 시간 |
| Weather & Sensor | 249px | 2 | 날씨 카드, 실시간 차압 정보 |
| Pressure Data | 680px | 3 | 히트맵, 압력 프로파일, 데이터 테이블 |
| Charts | 426px | 3 | 기압/기온/습도 라인 차트 |

### 간격 규칙

- **섹션 패딩**: 32px
- **카드 갭**: 24px
- **콘텐츠 패딩**: 24px
- **내부 갭**: 4px ~ 24px (세부 요소)

---

## 📊 데이터 테이블 구조

### 상세 데이터 테이블 (온도 데이터)

**컬럼 구성**:
```
┌───────┬──────────┬──────────┬──────────────┐
│  층   │ 실내온도 │ 통로온도 │ 실내/외온도차│
├───────┼──────────┼──────────┼──────────────┤
│  8    │ 24.9°C   │ 24.9°C   │ 24.9°C       │
│  7    │ 24.9°C   │ 24.9°C   │ 24.9°C       │
│  ...  │  ...     │  ...     │  ...         │
│  1    │ 24.9°C   │ 24.9°C   │ 24.9°C       │
└───────┴──────────┴──────────┴──────────────┘
```

**스타일**:
- 헤더: 40px, 회색 배경 (`#e0e5ea`), 12px 폰트
- 행: 56px, 흰색 배경 (`#ffffff`), 12px 폰트
- 테두리: 0.5px 상단 회색선 (`#aeb9c2`)

---

## 📈 차트 컴포넌트 정보

### 라인 차트 (3가지: 기압, 기온, 습도)

**특징**:
- 시간대: 10:00 ~ 15:00 (30분 간격)
- 이중 라인: 통로 vs 실내
- 색상: 
  - 통로: `#007bee` (파란색)
  - 실내: `#84deea` (밝은 파란색)

**단위**:
- 기압: Pa (파스칼)
- 기온: °C (섭씨)
- 습도: % (백분율)

**Y축 범위**: -10 ~ 30

---

## 🔄 상태 표시 시스템

### 연돌 발생 현황

```
Normal State:
- 값: 3.22Pa
- 배지: "정상" (초록색 배경)
- 추가 정보: "최대 발생 층 : 5F"

Active State:
- 값: 표시
- 배지: "활성화" (초록색 배경)
```

### 디바이스 정보

```
활성화율: 24/24 (98.5%)
- 값: "24/24"
- 상태: "활성화" (초록색 배지)
```

---

## 🎯 컴포넌트 사용 가이드

### 프로젝트 구조

```
src/
├── components/
│   ├── index.ts           # 컴포넌트 export
│   ├── Tag.tsx            # Tag 컴포넌트
│   ├── DataCard.tsx       # DataCard 컴포넌트
│   ├── StatusBadge.tsx    # StatusBadge 컴포넌트
│   ├── WeatherCard.tsx    # WeatherCard 컴포넌트
│   └── Heatmap.tsx        # Heatmap 컴포넌트
└── pages/
    └── Dashboard.tsx      # 대시보드 페이지
```

### Import 방법

```typescript
// 개별 import
import { Tag } from '@/components/Tag';
import { DataCard } from '@/components/DataCard';

// 또는 index.ts에서 한 번에
import { Tag, DataCard, StatusBadge, WeatherCard, Heatmap } from '@/components';
```

---

## 🎨 스타일링 가이드

### Tailwind CSS 변환

Design Tokens의 색상을 Tailwind CSS로 매핑:

```typescript
// tailwind.config.ts
export default {
  theme: {
    extend: {
      colors: {
        'primary-90': '#004594',
        'primary': '#007bee',
        'text-primary': '#131416',
        'text-secondary': '#464c53',
        'bg-card': '#ffffff',
        'bg-subtle': '#f4f5f6',
        'success': '#28cf47',
        // ... 기타 색상
      },
      spacing: {
        'gap-section': '24px',
        'padding-card': '24px',
      },
      borderRadius: {
        'xxl': '12px',
        'lg': '8px',
      },
    },
  },
};
```

---

## 📁 참고 파일

- **Figma 파일**: https://www.figma.com/design/rRI5RbFDfeZV9X8wMdTcz9/
- **대시보드 프레임 ID**: 19:4148
- **Design Tokens 파일**: `design-tokens.tokens.json`
- **추출 데이터**: `figma-dashboard-data.json`

---

## 📝 추가 노트

1. **이미지 자산**: 모든 이미지는 Figma CDN에서 제공되며 7일간 유효합니다.
2. **폰트**: Pretendard, Spoqa Han Sans Neo 사용
3. **반응형**: 현재 1920px 고정 너비로 설계됨
4. **색상 접근성**: WCAG AAA 레벨 충족하는 색상 대비 비율 사용

---

**생성 날짜**: 2026년 1월 19일
**원본 파일**: Figma - 삼성-연돌시스템-디자인
**추출 대상**: 대시보드 프레임 (Node ID: 19:4148)
